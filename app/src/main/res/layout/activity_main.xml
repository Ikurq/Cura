<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_cyber_deep_blue"
        android:orientation="vertical"
        android:clipChildren="false"
        android:clipToPadding="false">

        <!-- Unified Toolbar (Hidden on Home) -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:minHeight="?attr/actionBarSize"
            android:paddingTop="24dp"
            android:visibility="gone"
            app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            app:titleTextAppearance="@style/ToolbarTitle"
            app:titleTextColor="@color/text_primary" />

        <FrameLayout
            android:id="@+id/mainContentFrame"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipChildren="false"
            android:clipToPadding="false">

            <!-- Launcher Layout (Home) -->
            <FrameLayout
                android:id="@+id/launcherLayout"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <com.example.voicevox.ParticleBackgroundView
                    android:layout_width="match_parent"
                    android:layout_height="match_parent" />

                <androidx.constraintlayout.widget.ConstraintLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent">

                    <!-- Top Bar (Flavor HUD) -->
                    <LinearLayout
                        android:id="@+id/topSystemBar"
                        android:layout_width="match_parent"
                        android:layout_height="90dp"
                        android:background="#88000000"
                        android:gravity="bottom"
                        android:orientation="horizontal"
                        android:paddingHorizontal="16dp"
                        android:paddingBottom="12dp"
                        app:layout_constraintTop_toTopOf="parent">

                        <LinearLayout
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_weight="1"
                            android:orientation="vertical">
                            <TextView
                                android:id="@+id/welcomeTitle"
                                style="@style/CyberTypography.Label"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text="CURA_SYSTEM: ACTIVE"
                                android:textColor="@color/cyber_cyan"
                                android:textSize="12sp"
                                android:textStyle="bold" />
                            <TextView
                                android:id="@+id/hudFlavorText"
                                style="@style/CyberTypography.Header"
                                android:layout_width="match_parent"
                                android:layout_height="wrap_content"
                                android:text="MEMORY SEA: STABLE"
                                android:textColor="@color/white"
                                android:alpha="0.8"
                                android:textSize="16sp"
                                android:ellipsize="marquee"
                                android:marqueeRepeatLimit="marquee_forever"
                                android:scrollHorizontally="true"
                                android:singleLine="true" />
                        </LinearLayout>

                        <ImageButton
                            android:id="@+id/btnEmergencyStop"
                            android:layout_width="44dp"
                            android:layout_height="44dp"
                            android:layout_marginStart="12dp"
                            android:background="?attr/selectableItemBackgroundBorderless"
                            android:padding="8dp"
                            android:scaleType="fitCenter"
                            android:src="@android:drawable/ic_lock_power_off"
                            app:tint="@color/error" />

                        <ImageButton
                            android:id="@+id/btnQuickSettings"
                            android:layout_width="44dp"
                            android:layout_height="44dp"
                            android:layout_marginStart="4dp"
                            android:background="?attr/selectableItemBackgroundBorderless"
                            android:padding="8dp"
                            android:scaleType="fitCenter"
                            android:src="@android:drawable/ic_menu_preferences"
                            app:tint="@color/cyber_cyan" />
                    </LinearLayout>

                    <include
                        android:id="@+id/homeContent"
                        layout="@layout/layout_home_content"
                        android:layout_width="match_parent"
                        android:layout_height="0dp"
                        app:layout_constraintBottom_toBottomOf="parent"
                        app:layout_constraintTop_toBottomOf="@id/topSystemBar" />

                </androidx.constraintlayout.widget.ConstraintLayout>
            </FrameLayout>

            <!-- Fragment Container -->
            <FrameLayout
                android:id="@+id/fragmentContainer"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone" />

        </FrameLayout>

        <!-- Custom Multi-Layer Navigation Section -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="150dp"
            android:layout_marginHorizontal="12dp"
            android:layout_marginBottom="8dp"
            android:clipChildren="false"
            android:clipToPadding="false">

            <!-- Layer 1 (Bottom): The White Sliding Circle (The Bulge) -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/navBulgeCircle"
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:layout_gravity="bottom|start"
                android:layout_marginBottom="65dp"
                app:cardBackgroundColor="#FFFFFFFF"
                app:cardCornerRadius="35dp"
                app:cardElevation="0dp"
                android:elevation="0dp"
                app:strokeWidth="0dp" />

            <!-- Layer 2 (Middle): The Main White Bar -->
            <View
                android:layout_width="match_parent"
                android:layout_height="80dp"
                android:layout_gravity="bottom"
                android:layout_marginBottom="30dp"
                android:background="@drawable/bg_nav_white_base"
                android:elevation="0dp" />

            <!-- Layer 3 (Top): Invisible Navigation Logic / Icons -->
            <!-- Balanced padding to prevent both top and bottom clipping -->
            <com.google.android.material.bottomnavigation.BottomNavigationView
                android:id="@+id/bottomNavigation"
                style="@style/CyberBottomNavigation"
                android:layout_width="match_parent"
                android:layout_height="140dp"
                android:layout_gravity="bottom"
                android:background="@android:color/transparent"
                android:elevation="20dp"
                android:translationZ="20dp"
                android:clipChildren="false"
                android:clipToPadding="false"
                app:backgroundTint="@android:color/transparent"
                app:itemIconTint="@color/selector_nav_item_color"
                app:itemTextColor="@color/selector_nav_item_color"
                app:itemPaddingTop="44dp"
                app:itemPaddingBottom="16dp"
                app:labelVisibilityMode="labeled"
                app:menu="@menu/bottom_nav_menu" />
        </FrameLayout>

        <!-- Bottom Log Strip (DELETED - Providing Premium White Space) -->

    </LinearLayout>

    <!-- Loading Overlay -->
    <FrameLayout
        android:id="@+id/initialLoadingOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#0F172A"
        android:clickable="true"
        android:elevation="100dp"
        android:focusable="true">
        <ImageView android:id="@+id/initialCharacterImage" android:layout_width="wrap_content" android:layout_height="match_parent" android:layout_gravity="center" android:adjustViewBounds="true" android:alpha="0.2" android:scaleType="fitCenter" android:src="@drawable/guardian_character" />
        <LinearLayout android:layout_width="match_parent" android:layout_height="match_parent" android:gravity="bottom" android:orientation="vertical" android:padding="32dp">
            <TextView android:id="@+id/loadingLogText" android:layout_width="match_parent" android:layout_height="200dp" android:alpha="0.8" android:fontFamily="monospace" android:text="" android:textColor="@color/cyber_cyan" android:textSize="12sp" />
            <ProgressBar android:id="@+id/loadingProgressBar" style="?android:attr/progressBarStyleHorizontal" android:layout_width="match_parent" android:layout_height="4dp" android:layout_marginTop="16dp" android:progress="0" android:progressDrawable="@drawable/progress_mp" />
            <TextView android:id="@+id/loadingStatusText" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_marginTop="8dp" android:fontFamily="monospace" android:text="INITIALIZING..." android:textColor="@color/cyber_cyan" android:textSize="14sp" android:textStyle="bold" />
        </LinearLayout>
    </FrameLayout>

</androidx.drawerlayout.widget.DrawerLayout>
